name = "peggy"
version = "1.0.0"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
description = "Helper for working with FFmmpeg in Gleam"
licences = ["Apache-2.0"]
repository = { type = "github", user = "jackjohn7", repo = "peggy" }
# links = [{ title = "Website", href = "https://gleam.run" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

[dependencies]
gleam_stdlib = ">= 0.37.0 and < 1.0.0"
gleam_erlang = ">= 0.25.0 and < 1.0.0"
erlexec = ">= 2.0.6 and < 3.0.0"
simplifile = ">= 1.7.0 and < 2.0.0"
shellout = ">= 1.6.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
